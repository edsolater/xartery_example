import { useClick } from '@edsolater/hookit'
import { useState } from 'react'
import { useList } from './dataHandler'

function App() {
  const list = useList()
  return <div className='App'>hello xartery</div>
}

export default App
